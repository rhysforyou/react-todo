env:
  BUILDKITE_DOCKER: true

steps:
  - command: yarn test
    label: ":jest: Run Tests"
    agents:
      - "docker=*"
      - "queue=beta"
    plugins:
      docker-compose#v1.8.0:
        run: app
        env:
          - 'CI=true'
